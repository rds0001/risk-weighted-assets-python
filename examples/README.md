# Examples

`basic_api.py` exports the packaged reference workspace and calculates the universal-bank
dataset through the stable public API. Run it only in a directory where creating
`./rwa-workspace` is intended.
